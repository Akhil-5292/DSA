function reverse(arr){
    let i=0,j=arr.length-1
    while(i<j){
        arr[i]=arr[i]+arr[j]
        arr[j]=arr[i]-arr[j]
        arr[i]=arr[i]-arr[j]
        i++
        j--
    }
    return arr
}
let arr=[12,5,6,8,10]
console.log(reverse(arr));